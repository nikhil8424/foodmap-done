# FoodMap Backend

Node.js, Express.js, MongoDB Atlas (Mongoose), Socket.IO real-time backend for FoodMap.

## Setup & Run

```bash
cd backend
npm install
npm run dev
```

Server runs on: `http://localhost:3000`
