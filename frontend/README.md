# FoodMap Frontend

Vue 3, Tailwind CSS, Pinia, Vue Router, Axios, and Socket.IO Client.

## Setup & Run

```bash
cd frontend
npm install
npm run dev
```

Frontend runs on: `http://localhost:5000`
Proxying backend API on: `http://localhost:3000`
